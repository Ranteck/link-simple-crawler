[project]
name = "scrapper-links"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bs4>=0.0.2",
    "requests>=2.33.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.13",
]
